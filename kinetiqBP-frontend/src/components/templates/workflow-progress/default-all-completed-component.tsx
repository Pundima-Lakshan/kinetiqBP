import { ContainerBox } from '@/components/atoms';

export const DefaultAllCompletedComponent = () => {
  return <ContainerBox style={{ display: 'flex', justifyContent: 'center', alignItems: 'center' }}>All steps are completed</ContainerBox>;
};
