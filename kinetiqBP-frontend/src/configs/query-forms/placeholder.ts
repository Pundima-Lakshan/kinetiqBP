import { FormSchema } from '@/services';

export const getPlaceHolderFormSchema = () => {
  return {
    components: [],
    schemaVersion: 1,
    type: 'default',
    id: 'Form_chsdfrtConfig',
    versionTag: 'v1',
  } as FormSchema;
};
